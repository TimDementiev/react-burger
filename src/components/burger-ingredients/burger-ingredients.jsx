import burgerIngredientsStyles from "./burger-ingredients.module.css";

const burgerIngredients = () => {
  return (
    <section
      className={`${burgerIngredientsStyles.section} ml-5 pt-25`}
    ></section>
  );
};

export default burgerIngredients;
